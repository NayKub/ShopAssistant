import mongoose from 'mongoose';

const UserSchema = new mongoose.Schema({
    username: {
        type: String,
        required: [true, 'กรุณาใส่ชื่อผู้ใช้งาน (Username)'],
        trim: true,
        unique: true
    },
    email: {
        type: String,
        required: [true, 'กรุณาใส่อีเมล'],
        unique: true,
        match: [/.+@.+\..+/, 'กรุณาใส่อีเมลที่ถูกต้อง']
    },
    password: {
        type: String,
        required: [true, 'กรุณาใส่รหัสผ่าน'],
        minlength: [6, 'รหัสผ่านต้องมีอย่างน้อย 6 ตัวอักษร'],
        // 💡 ควรมีการ Hash รหัสผ่านก่อนบันทึกลง DB
    },
    // 🔑 FIELD สำคัญ: เชื่อมโยงผู้ใช้งานกับ _id ของสาขา
    store_id: {
        type: mongoose.Schema.Types.ObjectId, 
        ref: 'Store', // อ้างอิงถึงโมเดล Store
        required: [true, 'กรุณาระบุรหัสสาขา/ร้านค้าที่ผู้ใช้งานสังกัด']
    },
    // ฟิลด์ role ถูกลบออกตามคำขอ
}, 
{ 
    timestamps: true 
});

const User = mongoose.model('User', UserSchema);

export default User;